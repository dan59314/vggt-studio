# 專案協作與版本管理

本規則適用於此專案及其子目錄。回覆使用繁體中文。

## 改版完成後自動保存與推送

使用者已授權：每次完成使用者要求的功能改版或重要修正後，執行必要驗證，
將本次修改建立 Git 提交、附註版本標籤，並推送至既有 origin，無需重複詢問。
此規則由協作代理執行，不是背景監控、排程或 Git hook。
僅討論方案、回答問題、尚未完成或驗證失敗時，不發布版本。

### 發布流程

1. 先檢查 git status、目前分支、遠端與既有標籤，辨識使用者原有修改。
2. 完成工作並執行適合本次修改的建置與測試。純文件變更檢查內容即可。
3. 檢查差異與暫存內容，只提交本次任務的相關檔案；不得混入無關的使用者修改。
4. 建立清楚描述改動的提交。優先使用已設定的 Git 作者；若未設定，
   可僅針對本次提交使用 Codex <codex@localhost>，不得擅改全域作者設定。
5. 功能新增提升次版本（例如 v0.1.0 → v0.2.0）；修正提升修訂版本
   （例如 v0.2.0 → v0.2.1）。不相容變更需在版本說明中明確指出。
   純文件／協作規則修改可僅提交，不必新增發布標籤。
6. 使用尚未存在的 vMAJOR.MINOR.PATCH 附註標籤，描述版本主要內容。
   不得覆寫或移動既有標籤。
7. 執行 git push --follow-tags。若目前分支沒有追蹤遠端，僅推送本次分支
   並設定對應追蹤；不得推送所有分支或使用 force／force-with-lease／mirror。
8. 確認遠端分支與本次標籤符合本機提交，回報版本、提交編號、驗證結果及推送狀態。

## 指定遠端

- origin：https://github.com/dan59314/vggt-studio.git
- 儲存庫為私人。不得擅自改為公開、修改遠端目的地或擴大存取權限。
- 若推送失敗、需要登入、遠端已有新提交或出現衝突，保留本機提交與標籤，
  清楚回報原因。不得強制覆蓋遠端或宣稱已備份。

## 追蹤範圍與安全回復

- 遵循 .gitignore：追蹤原始碼、專案設定、測試程式與文件。
- 不提交模型權重、Python 虛擬環境、第三方下載目錄、私人影像／影片、
  重建輸出、編譯產物、密碼、權杖或其他機密。
- 需要回復時優先用獨立 worktree 取回舊版，或使用 git revert 保留歷史。
  不得未經使用者明確要求就執行 reset --hard 或刪除使用者工作。
- 詳細操作參閱 VERSION_CONTROL.md。
